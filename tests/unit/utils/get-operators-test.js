import getOperators from '../../../utils/get-operators';
import { module, test } from 'qunit';

module('Unit | Utility | get operators');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getOperators();
  assert.ok(result);
});
